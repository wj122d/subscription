{
  "name": "subscription-converter",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd web-ui && npm start\" \"node server/index.js\"",
    "build": "cd web-ui && npm run build",
    "test": "cd web-ui && npm test"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^4.21.2",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0",
    "morgan": "^1.10.1"
  }
}
